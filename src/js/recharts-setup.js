/**
 * recharts-setup
 */

/* Make references to Recharts */
var ResponsiveContainer = window.Recharts.ResponsiveContainer;
var LineChart = window.Recharts.LineChart;
var Line = window.Recharts.Line;
